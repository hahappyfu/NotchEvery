# NotchEvery

macOS 刘海（Notch）驻留应用：把屏幕顶部刘海扩展为可交互面板，承载配额概览、文件暂存与设置。单 SwiftUI 面板，按「分区」切换内容。

## Language

### 面板结构

**刘海（Notch）**:
屏幕顶部摄像头挖槽区域；应用以贴顶无边框窗口覆盖其上。_Avoid_: 舌头（指虚影形态）

**虚影（Ghost）**:
鼠标悬停刘海时出现的舌头形临时放大形态，点击或拖入文件后才完整展开。
_Avoid_: hover 态、半开态

**面板（Panel）**:
展开态下用户看到的整块玻璃卡片（Nook 风骨架，2026-09-08 定稿：跟随系统深浅色），固定宽 520pt，高度随当前分区查表（概览 165、Token 224、设置 284）。
_Avoid_: 窗口（窗口高度跟随当前分区：展开按高度表、收起回 200pt 条带，不再恒定）

**分区（Zone）**:
面板内的一屏内容。现有两页：概览（normal）、Token（token，2026-09-08 决定新增，请求列表优先、mock 先行，见 ADR-0005）；设置（settings）由齿轮经 Popover 打开，不占分页。旧「菜单区」已并入设置区（2026-09），不再作为独立分区。两页高度终值见 ADR-0006。
_Avoid_: 页面、标签页（tab 指控件不指内容）

**选项卡（Tab Bar）**:
已删除（任务 8 方案 C 定案：TabBar 早删，头部行 gear 与根齿轮重复，挂载连 `HeaderProbe` 一起移除）。分页改由 dots 指示器 + 横扫/方向键切换。
_Avoid_: 菜单（历史包袱：「菜单」曾指旧菜单分区，易混淆）

**头部槽位（Header Slot）**:
已随头部行删除（任务 8 方案 C）；`headerSlotHeight = 29` 常量保留（他人在途测试锁定，不作语义用途）。

**内容区（Zone Content）**:
唯一随分区高度动画的区域；高度 = 分区高度 − 间距×2（上下 padding 20×2）。内容自然高超过槽位即「高度表脱节」，Debug 断言拦截、Release 裁剪。

**顶栏（Headline）**:
展开面板顶部与刘海同高的一条区域，点击等于切到下一分区（与右滑同语义）。

**高度表（zonePanelHeight）**:
分区 → 面板高度的查表（`.normal: 165, .token: 224, .settings: 284`，推导见 ADR-0006）。宽度全区锁定 520pt，保证分页切换不水平平移。

### 交互

**分区切换（Zone Switch）**:
点选项卡、横扫（三重守卫：展开态+鼠标在面板内+未拖文件）、左右方向键、顶栏点击均可触发；切换时面板高度动画到目标区高度。

**展开/收起（Open/Close）**:
点击或拖文件到刘海展开；点面板外或移开鼠标收起。展开、收起是两条不同弹簧曲线（openAnimation 0.28/0.82、closeAnimation 0.2/1.0）。

**过桥菊花（Bridge Spinner）**:
虚影→展开瞬间 150ms 的短菊花，作为视觉衔接。

### 约束

**选项卡钉死**:
选项卡在分区切换时垂直、水平位置恒定；任何分区内容变化不得推挤选项卡。（2026-09-07 工单意图，实现仍有垂直跳动缺陷）
