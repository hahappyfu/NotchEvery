# NotchEvery

macOS 刘海（Notch）驻留应用：把屏幕顶部刘海扩展为可交互面板，承载配额概览、文件暂存与设置。单 SwiftUI 面板，按「分区」切换内容。

## Language

### 面板结构

**刘海（Notch）**:
屏幕顶部摄像头挖槽区域；应用以贴顶无边框窗口覆盖其上。_Avoid_: 舌头（指虚影形态）

**虚影（Ghost）**:
鼠标悬停刘海时出现的临时放大形态（黑色，顶部圆角），点击或拖入文件后才完整展开。
_Avoid_: hover 态、半开态

**面板（Panel）**:
展开态下用户看到的整块黑色岛体（2026-09-11 黑岛重构，见 ADR-0010：恒黑不跟随系统深浅色），尺寸跟随当前分区内容自然大小（宽高自适应，有界，见 ADR-0008），四角连续曲率大圆角。顶边贴屏幕顶，只往下长。
_Avoid_: 窗口（窗口是贴顶全屏透明条，面板是其中居中的岛体；窗口尺寸跟随面板，不再恒定）、玻璃卡片（旧形态已废）

**中央禁放区（Notch Dead Zone）**:
面板顶部中央被物理挖槽挡住的区域，只画背景、不放任何内容，宽度 = 物理挖槽宽 + 两侧边距，高度 = 物理刘海高 + 8pt（逐屏动态取值）。
_Avoid_: 刘海安全区（旧统称，易与耳区混淆）

**耳区（Notch Ears）**:
中央禁放区左右两边可正常显示的区域，各分区自定单行内容、超长截断，高度与禁放区等高且跨页保留（空白页亦占位，切页顶部不跳）。现定：概览页留空只画背景，Token 页左耳当前供应商（cc-switch claude-desktop 槽的 is_current，切换跟变、查无隐藏）、右耳今日调用次数。
_Avoid_: 刘海安全区（旧统称）

**分区（Zone）**:
面板内的一屏内容。现有两页：概览（normal，只剩额度块居中，暂存盘已删）、Token（token，2026-09-08 决定新增；请求列表与 KPI 现由 UsageStore 读 cc-switch 真实统计，见 ADR-0005）；设置（settings）由右键菜单打开，不占分页（右上齿轮已删，双击唤出待优化）。旧「菜单区」已并入设置区（2026-09），不再作为独立分区。面板尺寸跟随各区内容（见 ADR-0008），不再有固定高度终值。
_Avoid_: 页面、标签页（tab 指控件不指内容）、子菜单（口语即分区内容，已对齐到本词）

**选项卡（Tab Bar）**:
已删除（任务 8 方案 C 定案：TabBar 早删，头部行 gear 与根齿轮重复，挂载连 `HeaderProbe` 一起移除）。分页改由 dots 指示器 + 横扫/方向键切换。
_Avoid_: 菜单（历史包袱：「菜单」曾指旧菜单分区，易混淆）

**指示器（Dots）**:
底部融入卡片的滑动胶囊点阵（macOS 原生细碎风，无悬浮容器）：非当前页 6×6 圆点（`primary.opacity(0.25)`），当前页由 7×6 胶囊以 spring 掠过点阵标亮（`primary.opacity(0.75)`），点距 6pt、与内容底距 10pt；点击圆点直达该页（与横扫同语义）。
_Avoid_: 旧「霜化玻璃悬浮胶囊」容器样式（浮岛感）、黑底胶囊（旧 `black 0.35` 底样式）、无动效等大圆点

**头部槽位（Header Slot）**:
已随头部行删除（任务 8 方案 C）；`headerSlotHeight = 29` 常量保留（他人在途测试锁定，不作语义用途）。

**内容区（Zone Content）**:
决定面板尺寸的内容本体；面板尺寸 = 内容自然尺寸经最小/最大钳制。内容超限时在内部滚动/裁剪，不得撑大外层。

**展示卡（Display Card）**:
只读信息的容器：实色淡底圆角卡片（概览页整行 `white 0.06` + cornerRadius 12，完整包裹配额数据区与拖放交互区，清单 05 起取代配额独占小卡）。虚线拖放区嵌在卡内，虚线仍是"可拖"的唯一语言。
_Avoid_: 配额独占小卡（01 票旧形，已取代）

**拖放区（Drop Zone）**:
可拖入文件的容器：虚线框（如右侧暂存盘）。看到虚线即表示可拖放。
_Avoid_: 实色底（那是展示卡的语言）

**顶栏（Headline）**:
展开面板顶部与刘海同高的一条区域，点击等于切到下一分区（与右滑同语义）。

### 交互

**分区切换（Zone Switch）**:
点选项卡、横扫（三重守卫：展开态+鼠标在面板内+未拖文件）、左右方向键、顶栏点击、点指示器均可触发；切页内容走方向性滑动过渡（由 lastSwipeDirection 驱动：next 新页从右进、旧页向左出，previous 反之；清单 03 返工，之前按页写死方向、方向状态零消费），曲线走切页专用快弹簧（pageAnimation，无过冲；.snappy 需 macOS 14+、部署目标 13 故用高阻尼 spring），外层裁剪防溢出（清单 05），面板宽高弹性动画到新区尺寸（见 ADR-0008）。

**展开/收起（Open/Close）**:
点击或拖文件到刘海展开；点面板外或移开鼠标收起。展开、收起是两条不同弹簧曲线（openAnimation 0.28/0.82、closeAnimation 0.2/1.0）。收起不重置分区，展开恢复上次所在分区（仅本次运行内记忆，重启回概览）。

**过桥菊花（Bridge Spinner）**:
虚影→展开瞬间 150ms 的短菊花，作为视觉衔接。

### 约束

**选项卡钉死**:
旧约束，已被 ADR-0008 解除：宽度不再锁定，分区切换时面板宽度允许变化；dots 在底部严格居中，不再要求跨区位置恒定。
