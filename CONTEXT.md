# NotchEvery

macOS 刘海（Notch）驻留应用：把屏幕顶部刘海扩展为可交互面板，承载配额概览、文件暂存与设置。单 SwiftUI 面板，按「分区」切换内容。

## Language

### 面板结构

**刘海（Notch）**:
屏幕顶部摄像头挖槽区域；应用以贴顶无边框窗口覆盖其上。_Avoid_: 舌头（指虚影形态）

**虚影（Ghost）**:
鼠标悬停刘海时出现的舌头形临时放大形态，点击或拖入文件后才完整展开。
_Avoid_: hover 态、半开态

**面板（Panel）**:
展开态下用户看到的整块玻璃卡片（Nook 风骨架，2026-09-08 定稿：跟随系统深浅色），固定宽 600pt，高度随当前分区查表（概览 195、设置 284）。
_Avoid_: 窗口（窗口高度跟随当前分区：展开按高度表、收起回 200pt 条带，不再恒定）

**分区（Zone）**:
面板内的一屏内容。现有两区：概览（normal）、设置（settings）。旧「菜单区」已并入设置区（2026-09），不再作为独立分区。
_Avoid_: 页面、标签页（tab 指控件不指内容）

**选项卡（Tab Bar）**:
面板顶部左上角的分段控件（概览｜设置），黑色胶囊跟随选中分区；跨分区切换时水平、垂直位置都必须恒定——由「头部槽位」结构保证，不依赖内容恰好撑满面板。
_Avoid_: 菜单（历史包袱：「菜单」曾指旧菜单分区，易混淆）

**头部槽位（Header Slot）**:
面板顶部 28pt 固定区域，只装选项卡；任何分区高度动画都不进入此槽（选项卡钉死的结构保证）。

**内容区（Zone Content）**:
头部槽位以下、唯一随分区高度动画的区域；高度 = 分区高度 − 28 − 间距×3。内容自然高超过槽位即「高度表脱节」，Debug 断言拦截、Release 裁剪。

**顶栏（Headline）**:
展开面板顶部与刘海同高的一条区域，点击等于切到下一分区（与右滑同语义）。

**高度表（zonePanelHeight）**:
分区 → 面板高度的查表（`.normal: 195, .settings: 284`）。宽度全区锁定 600pt，保证选项卡跨区不水平平移。

### 交互

**分区切换（Zone Switch）**:
点选项卡、横扫（三重守卫：展开态+鼠标在面板内+未拖文件）、左右方向键、顶栏点击均可触发；切换时面板高度动画到目标区高度。

**展开/收起（Open/Close）**:
点击或拖文件到刘海展开；点面板外或移开鼠标收起。展开、收起是两条不同弹簧曲线（openAnimation 0.28/0.82、closeAnimation 0.2/1.0）。

**过桥菊花（Bridge Spinner）**:
虚影→展开瞬间 150ms 的短菊花，作为视觉衔接。

### 约束

**选项卡钉死**:
选项卡在分区切换时垂直、水平位置恒定；任何分区内容变化不得推挤选项卡。（2026-09-07 工单意图，实现仍有垂直跳动缺陷）
