# 02-kpi-strip：KPI 单行聚合条

## 目标
表格上方加一行 4 指标胶囊：Tokens / 缓存率 / 调用 / 成本。

## 改动
- `TokenZoneView` 顶部加 KPI 行（mock 聚合值与表格自洽：38.8M / 94.6% / 527次 / $0.00）。
- 缓存率绿色，其余沿现有配色；高度计入内容槽（03 工单一起量）。

## 验证
- 切 token 区守卫不断言；四项与表格数据口径一致。

## 阻塞
- 被 01 阻塞。阻塞：03。
